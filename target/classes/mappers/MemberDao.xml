<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gg.gop.dao.MemberDao">
	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="MemberDto">
		INSERT INTO member_tb (email, password, username, profile)
		VALUES (#{email}, #{password}, #{username}, 'default')
	</insert>
	
	<!-- 이메일 중복 검사를 위한 카운트 조회 -->
	<select id="countByEmail" resultType="int" parameterType="String">
		SELECT COUNT(*) FROM member_tb WHERE email = #{email}
	</select>

	<!-- 로그인 -->
	<select id="getSecurityPw" parameterType="String" resultType="String">
		select password from member_tb where email=#{email}
	</select>
<!-- 회원정보 -->
	<select id="getMemberInfo" resultType="MemberDto">
		select email,password,username from member_tb where email=#{email};
	</select>
	<!-- 회원정보 변경 -->
	<update id="updateMemberProfile" parameterType="MemberDto">
		UPDATE member_tb
		<set>
			<if test="username != null">username = #{username},</if>
			<if test="profile != null">profile = #{profile},</if>
		</set>
		WHERE email = #{email}
	</update>
</mapper>