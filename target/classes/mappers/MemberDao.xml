<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gg.gop.dao.MemberDao">
<!-- 회원가입 -->
<insert id="insertMember"
		parameterType="com.gg.gop.dto.MemberDto">
		INSERT INTO member_tb (m_id, m_pw, m_name,email,deleteYn) 
		VALUES (#{m_id}, #{m_pw},#{m_name},#{email},false)
	</insert>

	<select id="findById" parameterType="String"
		resultType="com.gg.gop.dto.MemberDto">
		SELECT * FROM member_tb 
		WHERE m_id = #{m_id}
	</select>

	<!-- 로그인 -->
    <select id="findByLogin" parameterType="map" resultType="com.gg.gop.dto.MemberDto">
        SELECT * FROM member_tb
        WHERE m_id = #{m_id} AND m_pw = #{m_pw}
    </select>

	<!-- 회원정보조회 R -->
	<select id="getMemberById" parameterType="string"
		resultType="com.gg.gop.dto.MemberDto">
		SELECT m_id
		FROM member_tb
		WHERE m_id
	</select>


	<!-- 회원정보수정 U -->
	<update id="updateMemberInfo"
		parameterType="com.gg.gop.dto.MemberDto">
		UPDATE member_tb
		SET m_pw = #{m_pw}, m_name = #{m_name}
		WHERE m_id = #{m_id}
	</update>

	<!-- 탈퇴 D 가입된회원은 0 , 탈퇴면 1로 변경 -->
	<delete id="deleteMember">
		UPDATE member_tb
		SET delete_Yn=1
		WHERE m_id=#{m_id}
	</delete>

</mapper>