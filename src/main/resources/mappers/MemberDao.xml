<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gg.gop.dao.MemberDao">
	<!-- 회원가입 -->
	<insert id="insertMember"
		parameterType="com.gg.gop.dto.MemberDto">
		INSERT INTO member_tb
		(email,username,m_pw,role)
		VALUES
		(#{email},#{username},#{password},#{role})
	</insert>
	<!-- 로그인 -->
	<select id="login" parameterType="HashMap" resultType="boolean">
		select
		count(*) from member_tb where email=#{email} and m_pw=#{password}
	</select>

	<select id="getSecurityPw" parameterType="String"
		resultType="String">
		select password from member_tb
		where email=#{email}
	</select>

	<select id="getMemberInfo" resultType="MemberDto">
		select email,m_pw,username
		from member_tb
		where email=#{email};
	</select>
	<!-- 회원정보수정 U -->
	<update id="updateMemberInfo" parameterType="MemberDto">
		UPDATE member_tb
		SET m_pw=#{password}, username = #{username}
		WHERE email = #{email}
	</update>

	<!-- 탈퇴 D 가입된회원은 0 , 탈퇴면 1로 변경 -->
	<delete id="deleteMember">
		UPDATE member_tb
		SET status=1
		WHERE email=#{email}
	</delete>

</mapper>