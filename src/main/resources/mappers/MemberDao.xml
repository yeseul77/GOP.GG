<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gg.gop.dao.MemberDao">
	<!-- 회원가입 -->
<insert id="insertMember" parameterType="com.gg.gop.dto.MemberDto">
    INSERT INTO member_tb (email, username, password)
    VALUES (#{email}, #{username}, #{password})
</insert>
	<!-- 중복확인 -->
	<select id="idCheck" resultType="boolean">
		select count(*)
		from member_tb
		where email=#{email}
	</select>

	<!-- 로그인 -->
	<select id="login" parameterType="HashMap" resultType="boolean">
		select
		count(*) from member_tb where email=#{email} and password=#{password}
	</select>

	<select id="getSecurityPw" parameterType="String"
		resultType="String">
		select password from member_tb
		where email=#{email}
	</select>

	<select id="getMemberInfo" resultType="MemberDto">
		select
		email,password,username
		from member_tb
		where email=#{email};
	</select>


	<!-- 회원정보수정 U -->
	<update id="updateMemberInfo" parameterType="MemberDto">
		UPDATE member_tb
		SETpassword=#{password}, username = #{username}
		WHERE email = #{email}
	</update>

	<!-- 탈퇴 D 가입된회원은 0 , 탈퇴면 1로 변경 -->
	<delete id="deleteMember">
		UPDATE member_tb
		SET delete_Yn=1
		WHERE email=#{email}
	</delete>
	<!--회원정보 변경 -->
	<update id="updateMemberProfile"
		parameterType="com.gg.gop.dto.MemberDto">
		UPDATE member
		<set>
			<if test="username != null">
				username = #{username},
			</if>
			<if test="profile != null">
				profile = #{profile},
			</if>
		</set>
		WHERE email = #{email}
	</update>



</mapper>

